global:
  scrape_interval: 15s

storage:
  tsdb:
    retention.time: 7d # Keep only 7 days of data to save disk space
    retention.size: 2GB

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8888']
